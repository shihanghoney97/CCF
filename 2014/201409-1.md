```c++
#include <stdio.h>

int main()
{
	int n,sum=0;
	scanf("%d", &n);
	int a[n];
	for(int i=0; i<n; i++)
	{
		scanf("%d", &a[i]);
		for(int j=0; j<i; j++)
		{
			if(a[i]-a[j]==1 | a[j]-a[i]==1)
				sum++;
		}
	}
	printf("%d", sum);
	return 0;
}
```